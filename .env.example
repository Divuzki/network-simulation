# Network Simulation App Environment Variables

# Frontend Configuration
# Socket.IO server URL for real-time communication
VITE_SOCKET_URL=http://192.168.0.173:3003

# API base URL for REST endpoints
VITE_API_URL=http://192.168.0.173:3003/api

# Server Configuration
# Host to bind the server to (0.0.0.0 for all interfaces, 192.168.0.173 for local only)
HOST=0.0.0.0

# Port for the server to listen on
PORT=3003

# Development vs Production
NODE_ENV=development

# Instructions:
# 1. Copy this file to .env in the same directory
# 2. Modify the values as needed for your environment
# 3. For production, update URLs to match your deployed server
# 4. Never commit .env files to version control